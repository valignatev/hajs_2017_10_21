```
✕ has 0 talks if no talks was added (6ms)
  ✓ has 1 talk if only one was added
  ✓ has 4 talks if 4 was added (1ms)
  ✓ sorted by talk time automatically
  ● has 0 talks if no talks was added
    expect(received).toEqual(expected)
    Expected value to equal:
      []
    Received:
      []
    Difference:
    Compared values have no visual difference.
```
